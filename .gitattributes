# Dump fixtures are byte-exact test input, not just text. A CRLF checkout puts
# a stray \r inside any value that spans lines, so a parser that preserves what
# the dump actually contains then decodes a different string than the tests
# expect. Keep them LF on every platform.
examples/**/*.sql text eol=lf
